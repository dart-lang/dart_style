>>>
void main() {
  var result = condition
      ? doA(
          param1, // comment to force split
          param2,
        )
      : doB();
}
<<<
void main() {
  var result =
      condition
          ? doA(
            param1, // comment to force split
            param2,
          )
          : doB();
}