>>>
class A {
  foo() {
    // comment #1
    if (comment != null && comment.end == token.offset
        // comment #2
        && node.parent.beginToken != token) {
    }
  }
}
<<< 3.6
class A {
  foo() {
    // comment #1
    if (comment != null &&
        comment.end == token.offset
        // comment #2
        &&
        node.parent.beginToken != token) {}
  }
}
