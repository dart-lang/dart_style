>>>
main() {
  final a = (
    element,
    element,
  )
      .getter;

  final b = [
    element,
    element,
  ].getter;
}
<<< 3.7
main() {
  final a = (element, element).getter;

  final b = [element, element].getter;
}
