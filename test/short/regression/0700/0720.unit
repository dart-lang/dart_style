### This test used to test --fix behavior but since that feature was removed,
### the test now just verifies that the formatter preserves the original code.
>>>
@meta(const Foo(const []))
library foo;
<<<
@meta(const Foo(const []))
library foo;
>>>
@meta(const Foo(const []))
import 'foo.dart';
<<<
@meta(const Foo(const []))
import 'foo.dart';