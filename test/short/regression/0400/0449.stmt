>>> (indent 4)
    for (int i = 0; i < variables.length; ++i)
      performance.updateVariable(variables[i]);
<<< 3.6
    for (int i = 0; i < variables.length; ++i)
      performance.updateVariable(variables[i]);
