>>> (indent 2)
var aLocalVar12 = new APrettyClass(APrettyClass.aStaticMethodOnIt(anArgument));
<<< 3.7
  var aLocalVar12 = new APrettyClass(
    APrettyClass.aStaticMethodOnIt(anArgument),
  );
>>>
main() {
  variableName.methodName(
      level, span.message(error.message, color: colorOf(severity.name)));
}
<<< 3.7
main() {
  variableName.methodName(
    level,
    span.message(error.message, color: colorOf(severity.name)),
  );
}
