>>>
void main() {
  var result = condition
      ? doA(
          param1, // comment to force split
          param2,
        )
      : doB();
}
<<< 3.7
void main() {
  var result =
      condition
          ? doA(
            param1, // comment to force split
            param2,
          )
          : doB();
}
<<< 3.8
void main() {
  var result = condition
      ? doA(
          param1, // comment to force split
          param2,
        )
      : doB();
}
