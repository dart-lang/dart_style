>>>
import 'package:meta/meta.dart';

class Foo {
  final int bar;
  Foo({
    @required this.bar,
  });
}
<<< 3.7
import 'package:meta/meta.dart';

class Foo {
  final int bar;
  Foo({@required this.bar});
}
