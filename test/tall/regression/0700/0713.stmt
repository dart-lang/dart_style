>>>
String type = status == 'OK'
    ? 'notices'
    : status == 'NO' ? 'warnings' : status == 'BAD' ? 'errors' : '';
<<<
String type = status == 'OK'
    ? 'notices'
    : status == 'NO'
    ? 'warnings'
    : status == 'BAD'
    ? 'errors'
    : '';
<<< 3.7
String type =
    status == 'OK'
        ? 'notices'
        : status == 'NO'
        ? 'warnings'
        : status == 'BAD'
        ? 'errors'
        : '';
