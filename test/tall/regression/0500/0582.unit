>>>
void fn() {
  List<String> a;
  // NOTICE THE TYPE PARAMETER
  a.fold<int>(0, (memo, s) {
    return max(memo, s.length);
  });
}
<<< 3.7
void fn() {
  List<String> a;
  // NOTICE THE TYPE PARAMETER
  a.fold<int>(0, (memo, s) {
    return max(memo, s.length);
  });
}
